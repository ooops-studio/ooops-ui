name: Mutation testing

on:
  workflow_dispatch:
  schedule:
    - cron: '30 4 * * 1'

permissions:
  contents: read

jobs:
  primitives:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
          cache: pnpm
      - name: Install mutation testing dependencies
        # Root tooling and primitives only; editor manifests are outside this test suite.
        run: pnpm --filter @ooopsstudio/ooops-ui --filter '@ooopsstudio/ui-primitives...' install --frozen-lockfile
      - run: pnpm test:mutation
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mutation-report
          path: reports/mutation/
          if-no-files-found: ignore
          retention-days: 14
