# Public Package

Use this archetype for a publishable library with one public entrypoint.

Expected contract:

- required scripts: `typecheck`, `build`
- optional scripts: `test`, `size`, `publint`, `attw`
- publish surface defined by `exports`
- package-local config can stay minimal
