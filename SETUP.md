# Setup

Generated by `pnpm init:template`.

## Project

- Workspace: `@ooopsstudio/ooops-ui`
- Starter package: `@ooopsstudio/ui-primitives`
- Repository: `https://github.com/ooops-studio/ooops-ui`
- Preset: `public-strict`
- Publish auth preference: `trusted-publishing`
- Registry strategy: `npm`

## Enabled modules

- Dependency Cruiser (`dependency-cruiser`)
- License policy (`license-policy`)
- Package migration tools (`migration-tools`)
- Package readiness (`package-readiness`)
- Publish compatibility checks (`publint-attw`)
- Changesets release workflow (`release`)
- Renovate dependency automation (`renovate`)
- Security audit (`security-audit`)
- Size Limit (`size-limit`)

## Release checklist

- Configure npm trusted publishing or `NPM_TOKEN`.
- Protect `main` and require CI before merge.
- Run the Release workflow with `dry_run=true` before the first publish.

## CI and security checklist

- Production dependency audit enabled.
- License policy check enabled.
- Package readiness enabled in strict mode.
- GitHub Actions use version-pinned actions by default.

## Next steps

- Run `pnpm install`.
- Run `pnpm -w validate:ci`.
- If package readiness reports large intentional packages, add package-specific overrides in `package-readiness.config.json`.
- Review package README files and package metadata before the first release.
