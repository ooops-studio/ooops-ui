---
import {resolveUiMessages, type UiMessages} from '@ooopsstudio/ui-primitives'; import {serializeUiConfig} from './index';
type Props = {id: string; name?: string; label?: string; value?: number | null; defaultValue?: number | null; min?: number; max?: number; step?: number; clampOnBlur?: boolean; required?: boolean; disabled?: boolean; messages?: Partial<UiMessages>; class?: string};
const {id, name = id, label, value = null, defaultValue = value, min, max, step = 1, clampOnBlur = true, required = false, disabled = false, messages, class: className = ''} = Astro.props;
const uiMessages = resolveUiMessages(messages);
const config = serializeUiConfig({value, defaultValue, min, max, step, clampOnBlur});
---
<div class:list={['ooops-number-input', className]} data-ooops-number-root data-part="root">
  {label ? <label for={id} data-part="label">{label}</label> : null}<span data-part="input-shell"><button type="button" aria-label={uiMessages.decrease} disabled={disabled} data-part="decrement">−</button><input {id} {name} type="number" value={value ?? ''} {min} {max} {step} {required} {disabled} data-part="control" /><button type="button" aria-label={uiMessages.increase} disabled={disabled} data-part="increment">+</button></span>
  <script is:inline type="application/json" data-ooops-ui-config set:html={config} />
</div>
<script>import {installNumberInput} from '@ooopsstudio/ui-astro/runtime/forms'; installNumberInput();</script>
