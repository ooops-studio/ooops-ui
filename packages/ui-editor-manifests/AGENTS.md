# UI Editor Manifests Guidance

- Keep this package optional and free of UI runtime imports.
- Treat manifests as JSON-safe adapter metadata validated by `@ooopsstudio/editor-contracts`.
- Keep parity tests aligned with every advertised Astro, Svelte and scene adapter export.
